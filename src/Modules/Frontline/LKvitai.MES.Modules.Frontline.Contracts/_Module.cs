namespace LKvitai.MES.Modules.Frontline.Contracts;

/// <summary>
/// Placeholder marker type for the Frontline.Contracts project. This project
/// intentionally exposes no DTOs, events, or messages yet — it exists only so the
/// module can be wired into the solution and participate in CI while the Frontline
/// domain is scaffolded.
/// </summary>
public static class FrontlineContractsModuleMarker
{
    public const string Module = "Frontline";
}
