namespace LKvitai.MES.Modules.Sales.Contracts;

/// <summary>
/// Placeholder marker type for the Sales.Contracts project. This project intentionally
/// exposes no DTOs, events, or messages yet — it exists only so the module can be wired
/// into the solution and participate in CI while the Sales domain is scaffolded.
/// </summary>
public static class SalesContractsModuleMarker
{
    public const string Module = "Sales";
}
