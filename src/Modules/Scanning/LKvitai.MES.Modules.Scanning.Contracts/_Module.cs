namespace LKvitai.MES.Modules.Scanning.Contracts;

/// <summary>
/// Placeholder marker type for the Scanning.Contracts project. This project
/// intentionally exposes no DTOs, events, or messages yet — it exists only so the
/// module can be wired into the solution and participate in CI while the Scanning
/// domain is scaffolded.
/// </summary>
public static class ScanningContractsModuleMarker
{
    public const string Module = "Scanning";
}
