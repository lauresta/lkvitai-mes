@inject NavigationManager Navigation
@implements IDisposable

<MudNavMenu Class="pa-2" data-testid="warehouse-navmenu">
    <MudNavLink Href="/dashboard" Match="NavLinkMatch.All">
        <MudIcon Icon="@Icons.Material.Filled.Dashboard" Class="mr-2" />Dashboard
    </MudNavLink>

    @foreach (var grp in _sections)
    {
        <MudNavGroup Title="@grp.Label"
                     Icon="@grp.Icon"
                     Expanded="@(_openSection == grp.Key)"
                     ExpandedChanged="expanded => OnSectionExpandedChanged(grp.Key, expanded)">
            @foreach (var item in grp.Items)
            {
                <MudNavLink Href="@item.Href" Match="NavLinkMatch.Prefix">
                    <MudIcon Icon="@item.Icon" Class="mr-2" />@item.Label
                </MudNavLink>
            }
        </MudNavGroup>
    }
</MudNavMenu>

@code {
    private string? _openSection;
    private string _currentUri = string.Empty;

    private record NavItem(string Href, string Icon, string Label);
    private record NavSection(string Key, string Label, string Icon, NavItem[] Items);

    protected override void OnInitialized()
    {
        _currentUri = Navigation.Uri;
        Navigation.LocationChanged += OnLocationChanged;
        _openSection = FindSectionForUri(_currentUri);
    }

    private void OnLocationChanged(object? sender, Microsoft.AspNetCore.Components.Routing.LocationChangedEventArgs e)
    {
        _currentUri = e.Location;
        var active = FindSectionForUri(_currentUri);
        if (active is not null)
        {
            _openSection = active;
        }

        InvokeAsync(StateHasChanged);
    }

    private void OnSectionExpandedChanged(string key, bool expanded)
    {
        _openSection = expanded ? key : (_openSection == key ? null : _openSection);
    }

    private string? FindSectionForUri(string uri)
    {
        foreach (var section in _sections)
        {
            if (section.Items.Any(i => uri.Contains(i.Href, StringComparison.OrdinalIgnoreCase)))
            {
                return section.Key;
            }
        }

        return null;
    }

    public void Dispose()
    {
        Navigation.LocationChanged -= OnLocationChanged;
    }

    private static readonly NavSection[] _sections =
    [
        new("stock", "Stock", Icons.Material.Filled.Inventory2, [
            new("/available-stock", Icons.Material.Filled.Inventory2, "Available Stock"),
            new("/search-by-image", Icons.Material.Filled.PhotoCamera, "Search by Image"),
            new("/warehouse/stock/dashboard", Icons.Material.Filled.GridView, "Stock Dashboard"),
            new("/warehouse/stock/location-balance", Icons.Material.Filled.ViewColumn, "Location Balance"),
            new("/warehouse/stock/adjustments", Icons.Material.Filled.Tune, "Adjustments"),
            new("/reservations", Icons.Material.Filled.AssignmentTurnedIn, "Reservations"),
        ]),

        new("inbound", "Inbound", Icons.Material.Filled.MoveToInbox, [
            new("/warehouse/inbound/shipments", Icons.Material.Filled.MoveToInbox, "Inbound Shipments"),
            new("/warehouse/inbound/qc", Icons.Material.Filled.FactCheck, "Receiving QC"),
            new("/warehouse/putaway", Icons.Material.Filled.SouthEast, "Putaway"),
        ]),

        new("outbound", "Outbound", Icons.Material.Filled.Outbox, [
            new("/warehouse/sales/orders", Icons.Material.Filled.ReceiptLong, "Sales Orders"),
            new("/warehouse/sales/allocations", Icons.Material.Filled.Tune, "Allocations"),
            new("/warehouse/outbound/orders", Icons.Material.Filled.Outbox, "Outbound Orders"),
            new("/warehouse/outbound/dispatch", Icons.Material.Filled.LocalShipping, "Dispatch"),
            new("/warehouse/waves", Icons.Material.Filled.Layers, "Wave Picking"),
            new("/warehouse/picking/tasks", Icons.Material.Filled.Checklist, "Picking Tasks"),
            new("/warehouse/labels", Icons.Material.Filled.QrCode, "Labels"),
            new("/warehouse/cross-dock", Icons.Material.Filled.SwapHoriz, "Cross-Dock"),
            new("/warehouse/rmas", Icons.Material.Filled.RotateLeft, "RMAs"),
        ]),

        new("operations", "Operations", Icons.Material.Filled.Settings, [
            new("/warehouse/transfers", Icons.Material.Filled.SwapHoriz, "Transfers"),
            new("/warehouse/cycle-counts", Icons.Material.Filled.AssignmentTurnedIn, "Cycle Counts"),
            new("/warehouse/visualization/3d", Icons.Material.Filled.ViewInAr, "Warehouse Map"),
            new("/projections", Icons.Material.Filled.Hub, "Projections"),
        ]),

        new("finance", "Finance", Icons.Material.Filled.Payments, [
            new("/warehouse/valuation/dashboard", Icons.Material.Filled.Payments, "Valuation"),
            new("/warehouse/agnum/config", Icons.Material.Filled.MenuBook, "Agnum Config"),
            new("/warehouse/agnum/reconcile", Icons.Material.Filled.Calculate, "Agnum Reconcile"),
        ]),

        new("admin", "Admin", Icons.Material.Filled.AdminPanelSettings, [
            new("/admin/users", Icons.Material.Filled.Group, "Users"),
            new("/warehouse/admin/settings", Icons.Material.Filled.Settings, "Admin Settings"),
            new("/warehouse/admin/reason-codes", Icons.Material.Filled.Rule, "Reason Codes"),
            new("/warehouse/admin/approval-rules", Icons.Material.Filled.FactCheck, "Approval Rules"),
            new("/warehouse/admin/roles", Icons.Material.Filled.Badge, "Roles"),
            new("/warehouse/admin/api-keys", Icons.Material.Filled.Key, "API Keys"),
            new("/warehouse/admin/gdpr-erasure", Icons.Material.Filled.GppBad, "GDPR Erasure"),
            new("/warehouse/admin/audit-logs", Icons.Material.Filled.MenuBook, "Audit Logs"),
            new("/warehouse/admin/backups", Icons.Material.Filled.Backup, "Backups"),
            new("/warehouse/admin/retention-policies", Icons.Material.Filled.HourglassBottom, "Retention Policies"),
            new("/warehouse/admin/dr-drills", Icons.Material.Filled.HealthAndSafety, "DR Drills"),
            new("/warehouse/admin/serial-numbers", Icons.Material.Filled.QrCode, "Serial Numbers"),
            new("/warehouse/admin/lots", Icons.Material.Filled.Inventory2, "Lots"),
            new("/warehouse/admin/uom", Icons.Material.Filled.Straighten, "Units of Measure"),
            new("/admin/items", Icons.Material.Filled.Inventory, "Items"),
            new("/admin/suppliers", Icons.Material.Filled.LocalShipping, "Suppliers"),
            new("/admin/supplier-mappings", Icons.Material.Filled.Link, "Supplier Mappings"),
            new("/admin/locations", Icons.Material.Filled.Place, "Locations"),
            new("/admin/warehouses", Icons.Material.Filled.Warehouse, "Warehouses"),
            new("/admin/categories", Icons.Material.Filled.AccountTree, "Categories"),
            new("/admin/import", Icons.Material.Filled.UploadFile, "Import Wizard"),
            new("/warehouse/admin/layout-editor", Icons.Material.Filled.DashboardCustomize, "Layout Editor"),
        ]),

        new("reports", "Reports", Icons.Material.Filled.Assessment, [
            new("/reports/stock-level", Icons.Material.Filled.Assessment, "Stock Level"),
            new("/reports/receiving-history", Icons.Material.Filled.MoveToInbox, "Receiving History"),
            new("/reports/pick-history", Icons.Material.Filled.Checklist, "Pick History"),
            new("/reports/dispatch-history", Icons.Material.Filled.LocalShipping, "Dispatch History"),
            new("/reports/stock-movements", Icons.Material.Filled.SyncAlt, "Stock Movements"),
            new("/reports/traceability", Icons.Material.Filled.AccountTree, "Traceability"),
            new("/warehouse/compliance/lot-trace", Icons.Material.Filled.Schema, "Lot Traceability"),
            new("/reports/compliance-audit", Icons.Material.Filled.VerifiedUser, "Compliance Audit"),
            new("/warehouse/compliance/dashboard", Icons.Material.Filled.History, "Compliance Dashboard"),
        ]),

        new("analytics", "Analytics", Icons.Material.Filled.AutoGraph, [
            new("/analytics/fulfillment", Icons.Material.Filled.AutoGraph, "Fulfillment KPIs"),
            new("/analytics/quality", Icons.Material.Filled.HealthAndSafety, "Quality Analytics"),
        ]),
    ];
}
